<div id="gallery">
  <div class="page-header">
    <h1><%= @property.name %></h1>
  </div>

  <div class="page-header">
    <h2><%= @gallery.name %></h2>
  </div>

  <%= simple_format(@gallery.name) %>
</div>
